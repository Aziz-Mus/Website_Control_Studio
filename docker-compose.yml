version: '3.8'

services:
  backend:
    build: ./backend
    container_name: wcs_backend
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: wcs_frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend
